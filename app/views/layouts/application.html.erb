<!DOCTYPE html>
<html>
<head>
  <title>DocLinks</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class='jumbotron'>
  <div class="col-md-4"></div>
  <div class="col-md-4">
    <h1 class="text-center doc"><%= link_to "Doc Links", categories_path %></h1>
    <p class="description text-center">{ keep track of your favorite documentation links, blog posts, and code snippets }</p>
  </div>
  <div class="col-md-4 container">
    <% if current_user %>
      <p class="text-right">Logged in as <%= current_user.name %></p>
      <p class="text-right"><%= link_to "Log out", logout_path, :class => "text-right" %></p>
    <% else %>
      <p class="text-right"><%= link_to "login", login_path, :id => "login" %></p>
      <p class="text-right"><%= link_to "Create Profile",  signup_path, :id => "create-profile" %></p>
    <% end %>
  </div>
</div>

<%= flash[:alert] %>
<%= flash[:notice] %>



<div class="container">
  <%= yield %>
</div>

</body>
</html>
