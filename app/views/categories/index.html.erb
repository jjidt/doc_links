<%= link_to "Add a category", new_category_path, :id => "add-category" %>
<h3>Your categories</h3>
<div class="col-md-4 left-box">
  <% @categories.each do |category| %>
      <%= link_to category.name, category_path(category), :class => "list-item" %>
  <% end %>
</div>
